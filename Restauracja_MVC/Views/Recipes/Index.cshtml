@model IEnumerable<Restauracja_MVC.Models.Recipes.RecipesListItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MealID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IngredientIDlist)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MealID)
            </td>
            <td>
                <!--@Html.DisplayFor(modelItem => item.IngredientIDlist)--> @*Works like shit*@
                @foreach(var ingredient in item.IngredientIDlist)
                {
                    @ingredient.ToString()
                    <br>
                }

            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.MealID }) |
                @Html.ActionLink("Details", "Details", new { id = item.MealID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.MealID })
            </td>
        </tr>
}
    </tbody>
</table>
